{
  "name": "encode-img",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"tsc -w\" \"nodemon dir/dev\"",
    "start": "node dir/",
    "build": "tsc && npx pkg -t node16-win-x64 dir/index.js",
    "update-major": "npm version --force --no-git-tag-version major",
    "update-minor": "npm version --force --no-git-tag-version minor",
    "update-patch": "npm version --force --no-git-tag-version patch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0",
    "dotenv": "^16.0.3",
    "pkg": "^5.8.1"
  },
  "devDependencies": {
    "@types/node": "^18.15.9"
  }
}
